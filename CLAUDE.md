# MoSS

## Tests

`.venv/bin/python -m pytest`. See `docs/testing.md`.

## Agent skills

### Issue tracker

Issues live as GitHub issues in `luizfernandolj/MoSS`, managed via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

The five canonical triage roles, each label string equal to its name. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: `CONTEXT.md` and `docs/adr/` at the repo root. See `docs/agents/domain.md`.
